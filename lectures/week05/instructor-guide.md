# 5차시 강의 운영안 — 데이터 훑어보기와 정리(EDA)

## 수업 흐름

1. 지난 시간 복습: 4차시 조건 검색(`df[df["설비번호"]=="EQ-02"]`)을 다시 상기시키고, 오늘은 같은 기술로 "이상한 값"을 찾는다고 예고
2. 오늘의 질문 제시 — "코드를 한 줄도 쓰기 전에, 이 데이터에 어떤 문제가 있어 보이는지 먼저 확인할 수 있을까?"
3. Orange3 시연: File → Data Table → Feature Statistics로 결측 셀·극단값 훑어보기
4. 개념 설명: 결측값·중복값·입력 오류·이상값 구분, 제거 vs 대체, 평균 vs 중앙값 + 용어 카드
5. 강사 코드 시연(`describe()`로 숫자 요약 먼저 훑어보기 → `isna`, `dropna`, `fillna`,
   `duplicated`, `drop_duplicates`, 조건 검색으로 이상값 찾기 → `loc`로 값만 정정하는 대안 소개)
6. 휴식
7. 학생 실습: 결측값 세기 → 제거/대체 → 중복 제거 → 이상값 찾기·제거 → 정제 전후 비교
8. 도전 실습: 공정명 오타 통일(`unique()`, `str.strip()`, `replace()`)
9. 정리 퀴즈 + 과제 안내 + 다음 차시 예고

## 준비물
- 발표자용 Colab 계정, 프로젝터, Orange3 설치(README 5절, `docs/orange3-guide.html` 참고)
- `data/weekly/week05/week05_dirty_process_data.csv` (학생 각자 다운로드 준비)
- `data/data_dictionary/week05_dictionary.md`를 미리 한 번 읽어 의도적 오염 목록을 숙지

## 오늘 데이터 핵심 수치(실제 실행 결과, 강의 중 반드시 확인)
- 원본: **113행 9열**
- 결측값 총 **10건** — 온도_섭씨 3, 압력_Pa 2, 가스유량_slm 3, 합격여부 2 (결측이 있는 행은 9행)
- 완전 중복 행 **3건**
- 날짜 형식이 다른 `측정시간` **2건**(`2024/03/05`, `03-05-2024 10:00`)
- 400℃ 이상 극단 고온 **2건**(약 540.3℃, 557.7℃)
- 음수 압력 **2건**(약 -42.9, -27.0 Pa)
- 200 이상/음수 비정상 가스유량 **2건**(-5.0, 250.0 slm)
- 공정명 오타 **3건**(`중착`→증착, `포토공정`→포토, `식각 `(공백)→식각)
- `합격여부` 누락 **2건**
- 정제 파이프라인(합격여부 결측 제거 → 나머지 결측 중앙값 대체 → 중복 제거 → 이상값 제거) 적용 시
  최종 **102행, 결측 0건**
- 정제 전 평균 온도(113행): **약 304.9℃** / 정제 후 평균 온도(102행): **약 300.6℃** / 차이: **약 4.3℃**

## 강사 설명 포인트
- Orange3 Data Table을 먼저 보여줄 때, 결측 셀이 빈칸으로 표시되는 것과 Feature Statistics의
  Max 열에서 500이 넘는 값이 눈에 띄는 것을 학생들이 스스로 발견하게 유도한다.
- "결측값 = 칸이 비어 있음", "이상값 = 칸에 값은 있지만 말이 안 됨"이라는 구분을 칠판에 나란히
  적어 강조한다.
- `isna()`는 결측값만 잡아낸다는 점, 이상값(500℃, 음수 압력)은 `isna()`로 잡히지 않고 조건
  검색으로 따로 찾아야 한다는 점을 반드시 짚는다.
- 평균 대 중앙값 차이는 "반 학생 용돈" 비유로 설명하면 이해가 빠르다.
- `dropna()`/`fillna()`가 원본을 바꾸지 않는다는 점(반환값을 다시 저장해야 함)을 실습 전에 미리 경고한다.
- `describe()`를 가장 먼저 보여주며 "결측을 하나씩 세기 전에 숫자 요약부터 훑어보는 것이
  실무 습관"이라는 점을 짚는다. `count`가 전체 행 수보다 적은 열, `mean`이 `50%`(중앙값)보다
  훨씬 큰 열을 학생들이 스스로 짚어내도록 유도한다.
- 이상값 처리에서 "행 삭제"만 배우고 끝내지 말고, 4차시에서 배운 `loc`로 값만 정정하는
  `df.loc[조건, "열"] = 값` 예시를 함께 보여주며 "삭제 vs 정정" 판단 기준을 짚는다.

## 질문 예시
- "Orange3 Feature Statistics에서 온도_섭씨의 Max 값을 보고 무엇이 이상하다고 느꼈나요?"
- "이 데이터에서 `온도_섭씨` 열의 결측값은 몇 개일까요? 직접 `isna().sum()`으로 확인해봅시다."
- "정제 전 평균 온도가 304.9℃로 정상 범위(295~305℃)를 살짝 벗어난 것처럼 보이는데, 정말 공정에
  문제가 있었을까요, 아니면 다른 이유가 있을까요?"

## 예상 오개념
- 결측값을 0으로 착각 → `0`은 "측정된 값이 0"이고 `NaN`은 "측정 자체가 안 됨"이라는 차이를 명확히 한다.
- `dropna()`를 실행하면 원본이 자동으로 바뀐다고 오해 → `df = df.dropna()`처럼 재할당이 필요함을 강조.
- 이상값도 `isna()`로 잡힌다고 오해 → 이상값은 "값이 존재"하므로 조건 검색(`df["온도_섭씨"] > 400`)으로 찾아야 함을 실습으로 직접 보여준다.
- 중복 3건을 제거하면 행이 3장씩(총 6행) 사라진다고 오해 → 중복 쌍마다 "여분 1개"만 제거되어 행 개수가 정확히 3만큼만 줄어듦을 코드로 확인시킨다.

## 실습 중 오류 대응
- `KeyError`: `df.columns`로 정확한 열 이름을 다시 확인하게 한다(특히 `가스유량_slm`, `처리시간_sec` 철자).
- `fillna()`에 중앙값을 잘못 넣어 다른 열 전체가 바뀌는 경우: 반드시 `df["열이름"] = df["열이름"].fillna(값)` 형태로 열을 지정했는지 확인.
- 조건 검색 결과가 빈 표로 나오는 경우: 비교 연산자(`<`, `>`)와 열 이름 철자를 다시 확인하게 한다.

## 빠른 학습자용 추가 과제
- 공정명 오타(`중착`, `포토공정`, 공백이 붙은 `식각 `)를 `str.strip()`과 `replace()`로 통일한 뒤, 정제 전/후 `value_counts()` 결과 변화를 비교해보게 한다.
- 날짜 형식이 다른 `측정시간` 2건을 `df["측정시간"].str.len()`이나 조건 검색으로 직접 찾아보게 한다(6차시 시계열 그래프 예고).

## 느린 학습자용 최소 과제
- `df.isna().sum()`으로 결측값 개수만 확인하고, "어떤 열에 결측이 가장 많은지" 말로 설명할 수 있으면 충분하다.
- `dropna()`와 `fillna()` 중 하나만 골라 실행해보고, 실행 전후 `df.shape`가 어떻게 달라지는지 확인한다.

## 수업 마무리 질문
- "Orange3에서 눈으로 확인한 문제와 Pandas 코드로 찾은 문제가 같았나요?"
- "오늘 배운 결측값·중복값·이상값 중 실무에서 가장 자주 발생할 것 같은 문제는 무엇일까요?"
- "정제 전후 평균 온도가 4.3℃나 차이 났다는 사실을 보고 어떤 생각이 들었나요?"
